# Netlify configuration for Better French

[build]
  command = ""
  publish = "ai_engine_v2/website"

# Redirect all 404s to index.html (single-page app behavior)
[[redirects]]
  from = "/404"
  to = "/index.html"
  status = 200 

[build.environment]
  PYTHON_VERSION = "3.10" 